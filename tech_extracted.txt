 
  `n`n           EMR Multi-Tenant System – Technical Design & Database Design   `n  Title: Technical Design & Database Design – Multi-Tenant EMR System   `n`n           1. Architecture Overview   `n  The EMR system is a cloud-based, multi-tenant web application with a modular service-oriented architecture.   `n`n       Client: Web application built with React, using a responsive UI.   `n`n       API Gateway: Routes requests, enforces authentication, and applies tenant routing based on domain/subdomain and tokens.   `n`n       Backend Services: RESTful services for auth, EMR core, billing, inventory, reporting, and integration.   `n`n       Database: PostgreSQL with multi-tenant support using tenant identifiers and optional schema-per-tenant.   `n`n       Caching: Redis for sessions, tokens, and frequently accessed configuration.   `n`n       Integrations: FHIR/HL7 integration service for interoperability.   `n  High-level request flow:   `n`n       User accesses the EMR via a tenant-specific URL (e.g., tenant-hospital.example.com).   `n`n       API Gateway validates the domain, resolves the tenant, and forwards the request with   `n  tenant_id  `n   in a secure header or token.   `n`n       Auth service validates credentials and issues JWTs containing   `n  tenant_id  `n   and role.   `n`n       Application services handle business logic and access the database, always filtering by   `n  tenant_id  `n  .   `n`n           2. Multi-Tenancy Design   `n  2.1 Tenant Identification   `n`n       Each tenant has a unique identifier (  `n  tenant_id  `n  ).   `n`n       Tenant is resolved via subdomain, custom domain mapping, or login context.   `n  2.2 Data Isolation Strategy   `n`n       Shared database, shared schema with   `n  tenant_id  `n   column on tenant-scoped tables.   `n`n       Optional schema-per-tenant for high-value customers or regulatory constraints.   `n`n       Application-layer enforcement: all queries require   `n  tenant_id  `n   filter.   `n`n       Database-layer support: indexes on   `n  tenant_id  `n   and composite indexes where needed.   `n   2.3 Tenant Provisioning   `n`n       Admin UI or admin API to create a tenant: name, contact, subdomain, and configuration.   `n`n       Creation steps:   `n`n       Insert row into   `n  tenants  `n   table.   `n`n       Optionally create separate schema and seed with base data.   `n`n       Set up branding, default roles, and default admin user.   `n  2.4 Configuration & Customization   `n`n       Tenant configuration stored as structured JSON or in dedicated tables (e.g.,   `n  tenant_settings  `n  ).   `n`n       Brand assets (logo, theme   `n  colors  `n  ) stored per tenant.   `n`n       Feature toggles per tenant to enable/disable modules (e.g., inventory, telehealth).   `n`n           3. Application Components   `n  3.1 Frontend   `n`n       React application with routing per module (patients, appointments, EMR, billing, inventory, reports, admin).   `n`n       Tenant-specific theming loaded after login or from domain context.   `n`n       Calls REST APIs secured by JWT.   `n  3.2 Backend Services   `n`n       Auth Service: handles login, logout, token generation, password reset.   `n`n       EMR   `n  Service:  `n   manages patients, encounters, clinical documents, orders.   `n`n       Scheduling Service: manages appointments, queues, and calendars.   `n`n       Billing Service: manages invoices, payments, and claim statuses.   `n`n       Inventory   `n  Service:  `n   manages items, stock, and transactions.   `n`n       Reporting Service: aggregates data for dashboards and reports.   `n`n       Integration Service: provides FHIR/HL7 endpoints and external system integrations.   `n`n           4. Security Design   `n  4.1 Authentication & Authorization   `n`n       JWT-based authentication.   `n`n        Role-based access control with roles such as Admin, Doctor, Nurse, Front Office, Billing, Inventory.   `n`n       Roles and permissions are scoped to a tenant.   `n  4.2 Data Protection   `n`n       HTTPS for all external endpoints.   `n`n       Encrypted storage of credentials (hashed passwords) and sensitive configuration values.   `n`n       Strict validation of input to mitigate injection attacks.   `n  4.3 Auditing   `n`n       Tables to log key events: logins, logout, failed logins, data views, updates, deletes.   `n`n       Audit events include   `n  tenant_id  `n  ,   `n  user_id  `n  , timestamp, action, and affected entity.   `n`n           5. Database Design   `n  Relational database: PostgreSQL.   `n  5.1 Core Tables (Logical Model)   `n`n       tenants   `n`n       id (PK)   `n`n       name   `n`n       code (short code/identifier)   `n`n       subdomain   `n`n       config (JSONB)   `n`n       status (active, suspended)   `n`n     `n  created_at  `n `n`n     `n  updated_at  `n `n`n       users   `n`n       id (PK)   `n`n     `n  tenant_id  `n   (FK ? tenants.id)   `n`n       email   `n`n     `n  password_hash  `n `n`n     `n  full_name  `n `n`n        role (admin, doctor, nurse,   `n  front_office  `n  , billing, inventory, etc.)   `n`n     `n  is_active  `n `n`n     `n  created_at  `n `n`n     `n  updated_at  `n `n`n       patients   `n`n       id (PK)   `n`n     `n  tenant_id  `n   (FK ? tenants.id)   `n`n     `n  mrn  `n   (medical record number, unique per tenant)   `n`n     `n  first_name  `n `n`n     `n  last_name  `n `n`n     `n  date_of_birth  `n `n`n       gender   `n`n       phone   `n`n       email   `n`n       address   `n`n       allergies (JSONB or text)   `n`n     `n  created_at  `n `n`n     `n  updated_at  `n `n`n       encounters   `n`n       id (PK)   `n`n     `n  tenant_id  `n   (FK ? tenants.id)   `n`n     `n  patient_id  `n   (FK ? patients.id)   `n`n     `n  provider_id  `n   (FK ? users.id)   `n`n     `n  encounter_type  `n   (OPD, IPD, emergency, etc.)   `n`n     `n  visit_date  `n `n`n     `n  chief_complaint  `n `n`n       diagnosis (text or coded)   `n`n        notes (text)   `n`n       status (open, closed)   `n`n     `n  created_at  `n `n`n     `n  updated_at  `n `n`n       appointments   `n`n       id (PK)   `n`n     `n  tenant_id  `n   (FK ? tenants.id)   `n`n     `n  patient_id  `n   (FK ? patients.id)   `n`n     `n  provider_id  `n   (FK ? users.id)   `n`n     `n  scheduled_start  `n `n`n     `n  scheduled_end  `n `n`n       status (scheduled, completed, cancelled,   `n  no_show  `n  )   `n`n       reason   `n`n     `n  created_at  `n `n`n     `n  updated_at  `n `n`n       prescriptions   `n`n       id (PK)   `n`n     `n  tenant_id  `n   (FK ? tenants.id)   `n`n     `n  encounter_id  `n   (FK ? encounters.id)   `n`n     `n  drug_name  `n `n`n       dosage   `n`n       frequency   `n`n       duration   `n`n       instructions   `n`n     `n  created_at  `n `n`n     `n  updated_at  `n `n`n       invoices   `n`n        id (PK)   `n`n     `n  tenant_id  `n   (FK ? tenants.id)   `n`n     `n  patient_id  `n   (FK ? patients.id)   `n`n     `n  encounter_id  `n   (FK ? encounters.id, nullable for non-encounter charges)   `n`n     `n  invoice_number  `n `n`n     `n  total_amount  `n `n`n     `n  paid_amount  `n `n`n     `n  balance_amount  `n `n`n       status (draft, issued, paid,   `n  partially_paid  `n  , void)   `n`n     `n  created_at  `n `n`n     `n  updated_at  `n `n`n     `n  invoice_items  `n `n`n       id (PK)   `n`n     `n  tenant_id  `n   (FK ? tenants.id)   `n`n     `n  invoice_id  `n   (FK ? invoices.id)   `n`n       description   `n`n       code (procedure code, service code, etc.)   `n`n       quantity   `n`n     `n  unit_price  `n `n`n       amount   `n`n     `n  created_at  `n `n`n     `n  inventory_items  `n `n`n       id (PK)   `n`n     `n  tenant_id  `n   (FK ? tenants.id)   `n`n     `n  item_code  `n `n`n       name   `n`n       category   `n`n     `n   reorder_level  `n `n`n     `n  current_stock  `n `n`n       unit   `n`n     `n  created_at  `n `n`n     `n  updated_at  `n `n`n     `n  inventory_transactions  `n `n`n       id (PK)   `n`n     `n  tenant_id  `n   (FK ? tenants.id)   `n`n     `n  item_id  `n   (FK ? inventory_items.id)   `n`n     `n  transaction_type  `n   (issue, receipt, adjustment)   `n`n       quantity   `n`n       reference (invoice, encounter, etc.)   `n`n     `n  created_at  `n `n`n     `n  audit_logs  `n `n`n       id (PK)   `n`n     `n  tenant_id  `n   (FK ? tenants.id)   `n`n     `n  user_id  `n   (FK ? users.id)   `n`n       action (login, logout, create, update, delete, view, export)   `n`n     `n  entity_name  `n `n`n     `n  entity_id  `n `n`n       details (JSONB)   `n`n     `n  created_at  `n `n`n     `n  tenant_settings  `n `n`n       id (PK)   `n`n     `n  tenant_id  `n   (FK ? tenants.id)   `n`n       key   `n`n       value (JSONB or text)   `n`n     `n   created_at  `n `n`n     `n  updated_at  `n `n  5.2 Indexing Strategy   `n`n       Index on   `n`n  tenants.code  `n`n   and   `n`n  tenants.subdomain  `n`n  .   `n`n       Composite indexes:   `n`n     `n  patients(  `n`n  tenant_id  `n  ,   `n  mrn  `n  )   `n`n     `n  appointments(  `n`n  tenant_id  `n  ,   `n  provider_id  `n  ,   `n  scheduled_start  `n  )   `n`n     `n  encounters(  `n`n  tenant_id  `n  ,   `n  patient_id  `n  ,   `n  visit_date  `n  )   `n`n     `n  invoices(  `n`n  tenant_id  `n  ,   `n  patient_id  `n  )   `n  5.3 FHIR Mapping (Overview)   `n`n       patients ? FHIR Patient.   `n`n       encounters ? FHIR Encounter.   `n`n       prescriptions ? FHIR   `n  MedicationRequest  `n  .   `n`n       Observations (vitals, lab values) would map to FHIR Observation in future tables.   `n`n           6. Deployment & DevOps   `n`n       Containerized services using Docker.   `n`n       Orchestration with Kubernetes or a managed platform that supports scaling and rolling updates.   `n`n       CI/CD pipeline to build, test, and deploy changes with zero or minimal downtime.   `n`n       Environment separation: dev, test, staging, production.   `n`n       Monitoring and logging using appropriate tooling (e.g., Prometheus + Grafana, ELK stack)   `n `n`n     
